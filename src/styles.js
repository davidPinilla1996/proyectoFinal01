import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
      textoPrincipal: {
         color: "#fff",
         fontSize: 25
      },
      container: {
        flex: 1,
       backgroundColor: "#fff",
      },
      textoContainer: {
         display: "flex",
         alignItems: "center",
         marginTop: 70
      },
      image: {
        height: "100%",
      }
  });