import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
      textoPrincipal: {
         color: "#686868",
         fontSize: 30
      },
      textoContainer: {
         display: "flex",
         alignItems: "center",
         marginTop: 70
      },
      image: {
        height: "100%"
      }
  });